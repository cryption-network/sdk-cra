import React from "react";
import { CreatePool, CreatePoolButton } from "@cryption/dapp-factory-sdk";

const Pool = () => {
  return (
    <>
      <CreatePoolButton />
      <CreatePool />
    </>
  );
};
export default Pool;
